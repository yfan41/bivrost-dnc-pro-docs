---
title: "安全与注意事项"
description: 规定本说明书的提示分级、适用人员资质，以及运行安全、数据安全与部署安全的强制性要求
---

本章为强制性内容，适用于 DNC Pro 系统的全部安装、配置、操作与维护人员。
使用本系统前应完整阅读本章。

## 一 提示分级 {#levels}

本说明书采用三级提示，含义与使用场合按表 0-1 的规定。

<p class="tbl-caption">表 0-1　提示分级</p>

| 级别 | 含义 | 忽略后果 |
| --- | --- | --- |
| **警告** | 涉及人身安全、设备完好与不可逆数据损失的强制性要求 | 可能造成机床停机、工件报废、加工事故或程序永久丢失 |
| **注意** | 涉及操作正确性与数据一致性的强制性要求 | 可能造成操作失败、状态不一致或权限拒绝 |
| **说明** | 补充信息、边界条件与设计意图 | 不影响安全，但可能影响理解与判断效率 |

三级提示在正文中以提示框呈现，示例如下。

:::danger[警告]
警告级提示的内容为强制性要求，不得省略执行。
:::

:::caution[注意]
注意级提示的内容为强制性要求，忽略将导致操作结果不符合预期。
:::

:::note[说明]
说明级提示为补充信息。
:::

## 二 适用人员 {#personnel}

本系统的操作人员应符合表 0-2 规定的资质要求。角色与权限的完整定义见 [6.1](/reference/rbac/)。

<p class="tbl-caption">表 0-2　适用人员与资质要求</p>

| 角色 | 承担工作 | 资质要求 |
| --- | --- | --- |
| 管理员 | 系统部署、账号与权限配置、令牌与外发订阅管理、审计核查 | 具备 Linux 与容器运行环境的运维能力，熟悉本单位信息安全制度 |
| 编程员 | 程序上传、版本维护、机台分配、试制下发 | 具备数控编程资质，熟悉目标机床的控制系统特性 |
| 审批员 | 版本审批与放行 | 具备工艺或质量审核授权 |
| 操作工 | 已发布版本的下发、机边操作台使用 | 经本系统操作培训，具备目标机床的操作资质 |
| 只读 | 查阅程序、传输记录与审计日志 | 无特殊要求 |

:::danger[警告]
本系统对机床文件系统执行删除与写入操作。未取得机床操作资质的人员不得执行下发操作，
也不应被授予 `deployments.send.released` 或 `deployments.send.proveout` 权限。
:::

## 三 运行安全 {#operation}

### 三.1 下发前的确认 {#before-transfer}

:::danger[警告]
下发将删除机床上的目标文件后重新写入。执行下发前应确认目标机床未处于加工状态，
且目标程序未被机床调用。对正在运行的程序执行下发可能造成加工中断或工件报废。
:::

下发前应逐项确认：

a) 目标机台的分配关系正确，落地文件名与路径符合该机床的命名规则（见 [5.3](/guide/assignments/)）；

b) 待下发版本处于「已发布」状态，或为经批准的试制版本；

c) 机床处于待机状态，操作面板已切换至可接收外部程序的模式；

d) 该机台无未处置的机边修改记录（见 [5.7](/guide/drift/)）。

### 三.2 试制程序的管理 {#trials}

:::danger[警告]
试制版本未经完整审批流程，不得用于批量生产。
试制到期后系统不会自动撤回机床上的程序，应由责任人显式执行转正或撤回。
:::

试制的完整规程见 [5.5](/guide/trials/)。对量产节拍机，应在机组级别启用禁止试制。

### 三.3 回滚失败的处置 {#restore-failed}

下发事务在任一阶段失败时自动使用快照回滚。回滚结果为「回滚失败」时，机床上文件的实际内容不确定。

:::danger[警告]
回滚结果为「回滚失败」的文件处于内容未知状态。应在恢复生产前由具备机床操作资质的人员
到机边核对实际程序内容，确认无误后方可继续加工，不得依据系统显示的版本号直接投产。
:::

回滚失败的记录在设备页与机边操作台上持续显示，直至下一次成功的校验将其清除。
事务阶段与回滚规则见 [5.4](/guide/transfer/)。

### 三.4 机边修改 {#drift}

:::caution[注意]
本系统不会自动覆盖机床上的内容。滚动镜像捕获到的机边修改以待处置版本的形式排队，
应由工程师比对差异后决定采纳或驳回。将受控版本推回机床是一次独立的显式操作。
:::

## 四 数据安全 {#data}

### 四.1 凭据保管 {#credentials}

:::caution[注意]
首次启动生成的管理员随机密码仅出现在该次启动日志中。应在首次登录后立即修改，
并将 `DNCPRO__Bootstrap__AdminPassword` 从 `.env` 中移除。
:::

a) 网关密钥与服务令牌的明文仅在创建时显示一次，应按本单位密钥管理制度保管；

b) 服务令牌应按最小权限与最小范围授予，不再使用时应及时吊销；

c) 演示数据集中的账号与口令为公开示例值，不应用于生产环境。

### 四.2 审计与追溯 {#audit}

审计日志为只增不改的哈希链，记录每一次状态变更与每一次程序内容外流。

:::caution[注意]
审计日志是质量追溯的依据。不应通过直接操作数据库的方式修改或删除审计记录，
此类操作将导致哈希链完整性校验失败。
:::

### 四.3 备份 {#backup}

:::danger[警告]
blob 存储不可写时，界面显示一切正常，但每一次程序上传与每一次镜像捕获都会静默失败。
更换存储卷或调整目录权限后，应通过 `/readyz` 确认 blob-store 状态为 `ok`，并将该项纳入监控告警。
:::

数据库与 blob 存储应同时纳入定期备份，备份与恢复规程见 [6.4](/reference/operations/)。
关闭滚动镜像将同时放弃备份能力与机边修改检测能力。

## 五 部署安全 {#deployment}

a) 本系统按局域网内部署设计，可在完全断网条件下运行，不应直接暴露于公共网络；

b) 生产部署目标仅支持 Linux 主机上的 Docker 运行环境，不支持 Windows Server、WSL2 与 Docker Desktop；

c) 本系统与彼络物联网关之间的网络应保持可达。网络中断期间下发任务将失败，机床保持原有内容不变。

## 六 责任边界 {#scope}

本系统的功能边界如下，超出边界的事项由使用单位自行负责：

a) **不采集加工数据。** 产量、OEE、报警与刀具寿命由彼络物联网关及上层系统负责，本系统仅管理加工程序；

b) **不代替人员决策。** 到期的试制不会被自动撤回，机床上被修改的程序不会被自动覆盖，二者均停留在界面上等待人工处置；

c) **不校验程序正确性。** 本系统保证机床上的内容与受控版本一致，不判断该程序的工艺正确性与加工安全性；

d) **不提供云端服务。** 全部数据存储于使用单位自有主机。
